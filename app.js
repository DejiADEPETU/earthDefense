var ussAnorld = {
    hull : hullValue,
    firePower : fireValue,
    accuracy : accuValue,
    powerLevel : 0
};

let nx = 1;
while (counter < nx){            //    This while loop can create any number of aliens equal to userInput: nx;
let alienShip={
    hull[nx]:hullValue[nx],
    firePower[nx]:fireValue[nx],
    accuracy[nx]:accuValue[nx]   // this values will be randomize either directly in place or by a method OR callback
    powerLevel : 0    
}
}

//NOW:  Simulate FireXchange:   When a missile is launched, it's either a hit or a miss  (think binary, think switches)...
//   The goal here is to simulate an ACTION/REACTION_RESULT situation for every missile fired.

// function FireXchange() {


// Create Input buttons:
let fiRe = (document).createElement("input");

fiRe.setAttribute('type','submit');
fiRe.setAttribute('value','Launch Missille');
fiRe.setAttribute('onclick','randomizeAccuracy()');  //  #CallBack!
fiRe.setAttribute('form','myform');
document.body.appendChild(fire);






